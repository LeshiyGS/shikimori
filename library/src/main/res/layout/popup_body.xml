<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true"
        android:background="@android:color/white"
        android:clickable="true">

        <LinearLayout
            android:id="@+id/wrapBody"
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:minHeight="@dimen/defaultPaddingLarge"
                android:paddingLeft="@dimen/defaultPadding"
                android:paddingTop="@dimen/defaultPadding"
                android:paddingRight="@dimen/defaultPadding"
                android:paddingBottom="@dimen/defaultPaddingMini"
                android:orientation="horizontal">

                <TextView
                    android:id="@+id/title"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    style="@style/altarixUiText.TextTitle"
                    android:layout_marginEnd="@dimen/defaultPaddingLarge"
                    android:layout_marginRight="@dimen/defaultPaddingLarge"
                    android:layout_gravity="center_vertical" />

            </FrameLayout>

            <LinearLayout
                android:id="@+id/llBody"
                android:orientation="vertical"
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

            </LinearLayout>

        </LinearLayout>
    </ScrollView>

    <ImageView
        android:id="@+id/ivClose"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_action_close"
        android:layout_gravity="end"
        android:layout_marginRight="@dimen/defaultPadding"
        android:layout_marginTop="@dimen/defaultPadding"
        android:background="?attr/selectableItemBackground"
        android:elevation="1dp" />

    <ProgressBar
        android:id="@+id/pbLoader"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:visibility="gone"
        style="@style/progresbar"
        android:layout_gravity="center"
        />
</FrameLayout>